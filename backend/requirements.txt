# MAESTRO Backend Requirements — v4 FINAL
# ==========================================
# RULES:
# 1. NO torch, NO transformers, NO audiocraft locally — use HF API instead
# 2. numpy MUST be 1.x (librosa not yet compatible with 2.x)
# 3. ALL audio processing is lazy-imported (not at module level) to prevent OOM

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Audio processing (lazy-imported inside functions, never at module level)
librosa==0.10.2
soundfile==0.12.1
numpy==1.26.4
scipy==1.13.1
audioread==3.0.1

# HTTP (for HuggingFace MusicGen API calls, async)
httpx==0.27.0
aiofiles==23.2.1

# Supabase
supabase==2.5.1

# AI / OpenRouter (GURU)
openai==1.30.1

# Utilities
python-dotenv==1.0.1
pydantic==2.7.1
tenacity==8.3.0

